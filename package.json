{
  "name": "sundive",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "SUNDIVE — one-button comet racing. Hold to dive, release to soar; race the ghost down a canyon of light.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4320 --strictPort",
    "qa": "node qa/run.mjs",
    "shots": "node scripts/shots.mjs"
  },
  "dependencies": {
    "pixi.js": "^8.6.6"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "playwright": "^1.52.0",
    "typescript": "^5.6.3",
    "vite": "^6.3.5"
  }
}
